# Boboddy integration dummy repository

This is a fixture repository used by the `work` command integration tests.

Its contents are intentionally minimal: the AI agent responses are faked, so the
actual repository contents do not matter. It exists only so the runtime
environment has a valid git workspace with a `.devcontainer/devcontainer.json`
to launch.
