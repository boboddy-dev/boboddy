{/*
  Boboddy docs is dark-only: no light/auto theme to switch to, so the
  theme picker in the header and mobile menu is intentionally removed.
  See `ForcedDarkThemeProvider.astro` for the matching override that pins
  `data-theme="dark"` with no light-mode flash.
*/}
